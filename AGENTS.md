# AGENTS.md — claude-mcp-starter-kit

## Mission
Keep this repository a clean, reliable MCP starter for Claude.

## Rules
- Keep starter code minimal and production-safe by default.
- Avoid exposing secrets or environment values in logs/docs.
- Validate changes with `npm run smoke` and `npm run lint`.
- Preserve open-source standards files and issue templates.
